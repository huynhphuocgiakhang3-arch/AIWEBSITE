# Web basics

HTML defines document structure. CSS controls presentation and layout. JavaScript adds browser behavior. TypeScript adds static typing. React uses components. Next.js is a framework for React applications.

Responsive websites should work on mobile and desktop. Debugging should reproduce the issue, inspect the error, make a controlled change, then run the relevant tests again.
