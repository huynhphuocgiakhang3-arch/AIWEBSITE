# Continual learning

The model does not blindly learn from every input. Candidate knowledge passes a quality gate. Future training runs should compare the new checkpoint against the previous benchmark before promotion.
