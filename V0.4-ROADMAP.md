# HPGK AGENT v0.5 — scope

## Completed
- Premium, lightweight Web Studio branded HPGK AGENT
- No external fonts, images, animation libraries or AI API in the web bundle
- Responsive mobile/desktop UI
- Custom BPE tokenizer
- Random-init Transformer
- Dataset pipeline and learning gate from v0.3
- Independent dependency-light inference worker
- `/health` and `/generate` worker endpoints
- Checkpoint metadata and model loading

## Not connected yet
Vercel Web Studio intentionally does not call the worker in v0.5.

## Next
v0.5 can connect Web → secure inference worker, add streaming output, request authentication/rate limits, conversation context, code-task routing, and real coding evaluation.
