# Coding evaluation

V0.3 adds transparent benchmark cases and static smoke checks. It intentionally does not execute arbitrary generated code. A future sandbox must isolate filesystem, network, processes and time before execution tests are enabled.
