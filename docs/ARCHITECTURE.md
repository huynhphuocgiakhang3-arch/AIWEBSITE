# HPGK v1.2 Architecture
Experience → Context → Knowledge → Retrieval → Agent → Tools → Verification → Artifacts.
Model provider is an independent server-side layer. Knowledge and memory are not hard-coded into the model.
